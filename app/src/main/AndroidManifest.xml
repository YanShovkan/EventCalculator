<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.eventcalculator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EventCalculator"
        tools:ignore="Instantiatable">
        <activity
            android:name=".activities.Equipments"
            android:exported="true" />
        <activity
            android:name=".activities.Premises"
            android:exported="true" />
        <activity
            android:name=".activities.Personal"
            android:exported="true" />
        <activity
            android:name=".activities.Handouts"
            android:exported="true" />
        <activity
            android:name=".activities.Products"
            android:exported="true" />
        <activity
            android:name=".activities.Event"
            android:exported="true" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>